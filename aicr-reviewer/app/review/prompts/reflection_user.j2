## Merge Request

**Title**: {{ mr_title }}

**Description**:
{{ mr_description }}

## Initial automated review (JSON)

Verify each issue against the MR diff. Return a corrected JSON object with the same schema.

```json
{{ initial_review_json }}
```
